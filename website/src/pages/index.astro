---
import Layout from '../layouts/Layout.astro';

const baseUrl = import.meta.env.BASE_URL;
---

<Layout title="Feedo - Terminal RSS Reader">
  <section class="hero">
    <div class="hero-content">
      <h1>
        <span class="logo-emoji">📡</span>
        Feedo
      </h1>
      <p class="hero-tagline">
        A beautiful terminal RSS reader<br />
        <span class="highlight">built with Rust</span>
      </p>
      <p class="hero-description">
        Read your favorite feeds in the terminal with a gorgeous TUI,
        vim-style keybindings, and 15 stunning themes.
      </p>

      <div class="install-box">
        <div class="install-tabs">
          <button class="install-tab active" data-tab="homebrew">Homebrew</button>
          <button class="install-tab" data-tab="cargo">Cargo</button>
        </div>
        <div class="install-commands">
          <div class="install-command active" id="homebrew-cmd">
            <code>brew install ricardodantas/tap/feedo</code>
            <button class="copy-btn" data-copy="brew install ricardodantas/tap/feedo" aria-label="Copy to clipboard">
              <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2">
                <rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect>
                <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path>
              </svg>
              <span class="copy-text">Copy</span>
            </button>
          </div>
          <div class="install-command" id="cargo-cmd">
            <code>cargo install feedo</code>
            <button class="copy-btn" data-copy="cargo install feedo" aria-label="Copy to clipboard">
              <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2">
                <rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect>
                <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path>
              </svg>
              <span class="copy-text">Copy</span>
            </button>
          </div>
        </div>
      </div>

      <div class="hero-buttons">
        <a href={`${baseUrl}docs`} class="btn btn-primary">
          Get Started
          <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M5 12h14M12 5l7 7-7 7"/>
          </svg>
        </a>
        <a href="https://github.com/ricardodantas/feedo" target="_blank" rel="noopener" class="btn btn-secondary">
          <svg viewBox="0 0 24 24" width="18" height="18" fill="currentColor">
            <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
          </svg>
          View on GitHub
        </a>
      </div>
    </div>

    <div class="hero-demo">
      <div class="demo-window">
        <div class="demo-titlebar">
          <span class="dot red"></span>
          <span class="dot yellow"></span>
          <span class="dot green"></span>
          <span class="demo-title">feedo</span>
        </div>
        <div class="demo-content">
          <img src={`${baseUrl}screenshots/feedo-main.png`} alt="Feedo TUI interface" loading="lazy" />
        </div>
      </div>
    </div>
  </section>

  <section class="features" id="features">
    <div class="container">
      <h2>Features</h2>
      <div class="features-grid">
        <div class="feature-card">
          <div class="feature-icon">🎨</div>
          <h3>Beautiful TUI</h3>
          <p>Clean three-panel interface with rounded borders, smooth navigation, and gorgeous typography.</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">🔍</div>
          <h3>Feed Discovery</h3>
          <p>Auto-detect RSS/Atom feeds from any URL — just paste a website and Feedo finds the feed.</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">📴</div>
          <h3>Offline Mode</h3>
          <p>Articles cached locally — read without internet, read states persist across sessions.</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">📁</div>
          <h3>Smart Folders</h3>
          <p>Organize feeds into collapsible folders with custom emoji icons for easy navigation.</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">🔎</div>
          <h3>Instant Search</h3>
          <p>Find articles across all feeds with real-time fuzzy filtering and highlighted results.</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">⚡</div>
          <h3>Blazingly Fast</h3>
          <p>Async feed fetching with Tokio — no UI blocking ever. Written in 100% safe Rust.</p>
        </div>
      </div>
    </div>
  </section>

  <section class="screenshots">
    <div class="container">
      <h2>See It In Action</h2>
      <div class="screenshots-grid">
        <div class="screenshot-card">
          <div class="screenshot-window">
            <div class="demo-titlebar">
              <span class="dot red"></span>
              <span class="dot yellow"></span>
              <span class="dot green"></span>
              <span class="demo-title">feedo - search</span>
            </div>
            <img src={`${baseUrl}screenshots/feedo-search.png`} alt="Real-time search" loading="lazy" />
          </div>
          <h3>Real-Time Search</h3>
          <p>Find any article instantly across all your feeds with fuzzy matching.</p>
        </div>

        <div class="screenshot-card">
          <div class="screenshot-window">
            <div class="demo-titlebar">
              <span class="dot red"></span>
              <span class="dot yellow"></span>
              <span class="dot green"></span>
              <span class="demo-title">feedo - add feed</span>
            </div>
            <img src={`${baseUrl}screenshots/feedo-add-feed.png`} alt="Add feed dialog" loading="lazy" />
          </div>
          <h3>Smart Feed Discovery</h3>
          <p>Just paste any URL and Feedo will automatically find the RSS/Atom feed.</p>
        </div>
      </div>
    </div>
  </section>

  <section class="keybindings">
    <div class="container">
      <h2>Keyboard-Driven</h2>
      <p class="section-description">Vim-style keybindings for lightning-fast navigation</p>
      
      <div class="keybindings-grid">
        <div class="keybinding-group">
          <h3>Navigation</h3>
          <div class="keybinding-list">
            <div class="keybinding">
              <span class="keys"><kbd>j</kbd> <kbd>↓</kbd></span>
              <span>Move down</span>
            </div>
            <div class="keybinding">
              <span class="keys"><kbd>k</kbd> <kbd>↑</kbd></span>
              <span>Move up</span>
            </div>
            <div class="keybinding">
              <span class="keys"><kbd>l</kbd> <kbd>Enter</kbd></span>
              <span>Select / Enter</span>
            </div>
            <div class="keybinding">
              <span class="keys"><kbd>h</kbd> <kbd>←</kbd></span>
              <span>Go back</span>
            </div>
            <div class="keybinding">
              <span class="keys"><kbd>Tab</kbd></span>
              <span>Switch panels</span>
            </div>
          </div>
        </div>

        <div class="keybinding-group">
          <h3>Actions</h3>
          <div class="keybinding-list">
            <div class="keybinding">
              <span class="keys"><kbd>n</kbd></span>
              <span>Add new feed</span>
            </div>
            <div class="keybinding">
              <span class="keys"><kbd>r</kbd></span>
              <span>Refresh all</span>
            </div>
            <div class="keybinding">
              <span class="keys"><kbd>o</kbd></span>
              <span>Open in browser</span>
            </div>
            <div class="keybinding">
              <span class="keys"><kbd>/</kbd></span>
              <span>Search</span>
            </div>
            <div class="keybinding">
              <span class="keys"><kbd>t</kbd></span>
              <span>Theme picker</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="themes-preview">
    <div class="container">
      <h2>15 Beautiful Themes</h2>
      <p class="section-description">
        Press <code>t</code> to open the theme picker with live preview
      </p>
      
      <div class="theme-grid">
        <div class="theme-card">
          <div class="theme-colors">
            <span style="background: #bd93f9"></span>
            <span style="background: #ff79c6"></span>
            <span style="background: #8be9fd"></span>
            <span style="background: #50fa7b"></span>
          </div>
          <span class="theme-name">Dracula</span>
        </div>
        <div class="theme-card">
          <div class="theme-colors">
            <span style="background: #88c0d0"></span>
            <span style="background: #81a1c1"></span>
            <span style="background: #bf616a"></span>
            <span style="background: #a3be8c"></span>
          </div>
          <span class="theme-name">Nord</span>
        </div>
        <div class="theme-card">
          <div class="theme-colors">
            <span style="background: #7aa2f7"></span>
            <span style="background: #bb9af7"></span>
            <span style="background: #f7768e"></span>
            <span style="background: #9ece6a"></span>
          </div>
          <span class="theme-name">Tokyo Night</span>
        </div>
        <div class="theme-card">
          <div class="theme-colors">
            <span style="background: #89b4fa"></span>
            <span style="background: #f5c2e7"></span>
            <span style="background: #f38ba8"></span>
            <span style="background: #a6e3a1"></span>
          </div>
          <span class="theme-name">Catppuccin</span>
        </div>
        <div class="theme-card">
          <div class="theme-colors">
            <span style="background: #fabd2f"></span>
            <span style="background: #d3869b"></span>
            <span style="background: #fb4934"></span>
            <span style="background: #b8bb26"></span>
          </div>
          <span class="theme-name">Gruvbox</span>
        </div>
        <div class="theme-card">
          <div class="theme-colors">
            <span style="background: #00ffff"></span>
            <span style="background: #ff00ff"></span>
            <span style="background: #ff003c"></span>
            <span style="background: #00ff64"></span>
          </div>
          <span class="theme-name">Cyberpunk</span>
        </div>
      </div>
      
      <div class="themes-cta">
        <a href={`${baseUrl}themes`} class="btn btn-secondary">
          Browse All Themes
          <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M5 12h14M12 5l7 7-7 7"/>
          </svg>
        </a>
      </div>
    </div>
  </section>

  <section class="quick-start">
    <div class="container">
      <h2>Quick Start</h2>
      <div class="steps">
        <div class="step">
          <div class="step-number">1</div>
          <div class="step-content">
            <h3>Install Feedo</h3>
            <div class="step-code">
              <code>brew install ricardodantas/tap/feedo</code>
            </div>
          </div>
        </div>

        <div class="step">
          <div class="step-number">2</div>
          <div class="step-content">
            <h3>Launch the TUI</h3>
            <div class="step-code">
              <code>feedo</code>
            </div>
          </div>
        </div>

        <div class="step">
          <div class="step-number">3</div>
          <div class="step-content">
            <h3>Add your first feed</h3>
            <div class="step-code">
              <code>Press <kbd>n</kbd> and paste a URL</code>
            </div>
          </div>
        </div>
      </div>

      <div class="quick-start-cta">
        <a href={`${baseUrl}docs`} class="btn btn-primary">
          Read the Full Guide
          <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M5 12h14M12 5l7 7-7 7"/>
          </svg>
        </a>
      </div>
    </div>
  </section>
</Layout>

<style>
  /* Hero Section */
  .hero {
    padding: calc(var(--nav-height) + 4rem) 1.5rem 4rem;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 3rem;
    background: linear-gradient(180deg, var(--bg-secondary) 0%, var(--bg-primary) 100%);
  }

  .hero-content {
    text-align: center;
    max-width: 700px;
  }

  h1 {
    font-size: clamp(3rem, 8vw, 5rem);
    font-weight: 800;
    margin-bottom: 1rem;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
  }

  .logo-emoji {
    font-size: 0.8em;
  }

  .hero-tagline {
    font-size: clamp(1.25rem, 3vw, 1.5rem);
    color: var(--text-secondary);
    margin-bottom: 1rem;
    line-height: 1.4;
  }

  .highlight {
    color: var(--accent-purple);
  }

  .hero-description {
    font-size: 1.125rem;
    color: var(--text-secondary);
    margin-bottom: 2rem;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .install-box {
    display: inline-flex;
    flex-direction: column;
    align-items: stretch;
    background: var(--bg-secondary);
    border: 1px solid var(--bg-tertiary);
    border-radius: 8px;
    margin-bottom: 2rem;
    font-family: var(--font-mono);
    min-width: 320px;
  }

  .install-tabs {
    display: flex;
    border-bottom: 1px solid var(--bg-tertiary);
  }

  .install-tab {
    flex: 1;
    padding: 0.75rem 1rem;
    background: transparent;
    border: none;
    color: var(--text-secondary);
    font-family: var(--font-sans);
    font-size: 0.875rem;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.2s;
  }

  .install-tab:first-child {
    border-radius: 8px 0 0 0;
  }

  .install-tab:last-child {
    border-radius: 0 8px 0 0;
  }

  .install-tab:hover {
    color: var(--text-primary);
    background: var(--bg-tertiary);
  }

  .install-tab.active {
    color: var(--accent-purple);
    background: var(--bg-primary);
    border-bottom: 2px solid var(--accent-purple);
    margin-bottom: -1px;
  }

  .install-commands {
    padding: 0.75rem 1rem;
  }

  .install-command {
    display: none;
    align-items: center;
    gap: 0.5rem;
  }

  .install-command.active {
    display: flex;
  }

  .install-box code {
    background: none;
    padding: 0;
    color: var(--accent-green);
    font-size: 1rem;
  }

  .copy-btn {
    display: flex;
    align-items: center;
    gap: 0.25rem;
    background: var(--bg-tertiary);
    border: none;
    color: var(--text-secondary);
    padding: 0.5rem 0.75rem;
    border-radius: 4px;
    cursor: pointer;
    font-size: 0.75rem;
    font-family: var(--font-sans);
    transition: all 0.2s;
    margin-left: auto;
  }

  .copy-btn:hover {
    background: var(--accent-purple);
    color: var(--bg-primary);
  }

  .copy-btn.copied {
    background: var(--accent-green);
    color: var(--bg-primary);
  }

  .hero-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.875rem 1.5rem;
    border-radius: 8px;
    font-weight: 600;
    font-size: 1rem;
    transition: all 0.2s;
  }

  .btn-primary {
    background: var(--accent-purple);
    color: var(--bg-primary);
  }

  .btn-primary:hover {
    background: var(--accent-pink);
    color: var(--bg-primary);
    transform: translateY(-2px);
  }

  .btn-secondary {
    background: var(--bg-tertiary);
    color: var(--text-primary);
  }

  .btn-secondary:hover {
    background: var(--bg-tertiary);
    color: var(--accent-purple);
    transform: translateY(-2px);
  }

  .hero-demo {
    width: 100%;
    max-width: 900px;
  }

  .demo-window {
    background: var(--bg-secondary);
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
    border: 1px solid var(--bg-tertiary);
  }

  .demo-titlebar {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 12px 16px;
    background: var(--bg-tertiary);
  }

  .dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
  }

  .dot.red { background: #ff5f56; }
  .dot.yellow { background: #ffbd2e; }
  .dot.green { background: #27c93f; }

  .demo-title {
    margin-left: auto;
    margin-right: auto;
    font-family: var(--font-mono);
    font-size: 0.875rem;
    color: var(--text-secondary);
  }

  .demo-content img {
    width: 100%;
    display: block;
  }

  /* Features */
  .features, .screenshots, .keybindings, .themes-preview, .quick-start {
    padding: 5rem 1.5rem;
  }

  .container {
    max-width: var(--max-width);
    margin: 0 auto;
  }

  .features h2, .screenshots h2, .keybindings h2, .themes-preview h2, .quick-start h2 {
    font-size: 2.5rem;
    text-align: center;
    margin-bottom: 1rem;
  }

  .section-description {
    text-align: center;
    color: var(--text-secondary);
    font-size: 1.125rem;
    margin-bottom: 3rem;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    margin-top: 3rem;
  }

  .feature-card {
    background: var(--bg-secondary);
    border: 1px solid var(--bg-tertiary);
    border-radius: 12px;
    padding: 2rem;
    transition: transform 0.2s, border-color 0.2s;
  }

  .feature-card:hover {
    transform: translateY(-4px);
    border-color: var(--accent-purple);
  }

  .feature-icon {
    font-size: 2.5rem;
    margin-bottom: 1rem;
  }

  .feature-card h3 {
    font-size: 1.25rem;
    margin-bottom: 0.5rem;
  }

  .feature-card p {
    color: var(--text-secondary);
    line-height: 1.6;
  }

  /* Screenshots */
  .screenshots {
    background: var(--bg-secondary);
  }

  .screenshots-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
    gap: 2rem;
    margin-top: 3rem;
  }

  .screenshot-card {
    text-align: center;
  }

  .screenshot-window {
    background: var(--bg-primary);
    border-radius: 12px;
    overflow: hidden;
    border: 1px solid var(--bg-tertiary);
    margin-bottom: 1.5rem;
  }

  .screenshot-window img {
    width: 100%;
    display: block;
  }

  .screenshot-card h3 {
    font-size: 1.25rem;
    margin-bottom: 0.5rem;
  }

  .screenshot-card p {
    color: var(--text-secondary);
  }

  /* Keybindings */
  .keybindings-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 3rem;
    margin-top: 2rem;
  }

  .keybinding-group h3 {
    font-size: 1.25rem;
    margin-bottom: 1.5rem;
    color: var(--accent-purple);
  }

  .keybinding-list {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .keybinding {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0.75rem 1rem;
    background: var(--bg-secondary);
    border-radius: 8px;
    border: 1px solid var(--bg-tertiary);
  }

  .keys {
    display: flex;
    gap: 0.5rem;
  }

  kbd {
    background: var(--bg-tertiary);
    padding: 0.25rem 0.5rem;
    border-radius: 4px;
    font-family: var(--font-mono);
    font-size: 0.875rem;
    border: 1px solid var(--text-secondary);
  }

  /* Themes Preview */
  .themes-preview {
    background: var(--bg-secondary);
  }

  .theme-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
    gap: 1rem;
    margin-top: 2rem;
    margin-bottom: 2rem;
  }

  .theme-card {
    background: var(--bg-primary);
    border: 1px solid var(--bg-tertiary);
    border-radius: 8px;
    padding: 1rem;
    text-align: center;
    transition: transform 0.2s, border-color 0.2s;
  }

  .theme-card:hover {
    transform: translateY(-2px);
    border-color: var(--accent-purple);
  }

  .theme-colors {
    display: flex;
    gap: 0.5rem;
    justify-content: center;
    margin-bottom: 0.75rem;
  }

  .theme-colors span {
    width: 24px;
    height: 24px;
    border-radius: 50%;
  }

  .theme-name {
    font-size: 0.875rem;
    font-weight: 500;
  }

  .themes-cta {
    text-align: center;
  }

  /* Quick Start */
  .steps {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    max-width: 600px;
    margin: 3rem auto;
  }

  .step {
    display: flex;
    gap: 1.5rem;
    align-items: flex-start;
  }

  .step-number {
    width: 48px;
    height: 48px;
    background: var(--accent-purple);
    color: var(--bg-primary);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: 1.25rem;
    flex-shrink: 0;
  }

  .step-content h3 {
    font-size: 1.25rem;
    margin-bottom: 0.5rem;
  }

  .step-code code {
    font-size: 1rem;
    color: var(--accent-green);
  }

  .step-code kbd {
    background: var(--bg-tertiary);
    padding: 0.125rem 0.375rem;
    border-radius: 4px;
    font-size: 0.875rem;
  }

  .quick-start-cta {
    text-align: center;
  }

  /* Mobile */
  @media (max-width: 768px) {
    .hero {
      padding: calc(var(--nav-height) + 2rem) 1rem 2rem;
      gap: 2rem;
    }

    .hero-content {
      max-width: 100%;
    }

    h1 {
      font-size: 2.5rem;
      flex-wrap: wrap;
    }

    .install-box {
      width: 100%;
      max-width: 100%;
      min-width: unset;
    }

    .install-command {
      flex-wrap: wrap;
      justify-content: center;
      gap: 0.5rem;
    }

    .install-box code {
      font-size: 0.875rem;
      word-break: break-all;
    }

    .hero-buttons {
      flex-direction: column;
      width: 100%;
    }

    .btn {
      justify-content: center;
      width: 100%;
    }

    .features, .screenshots, .keybindings, .themes-preview, .quick-start {
      padding: 3rem 1rem;
    }

    .features-grid {
      grid-template-columns: 1fr;
    }

    .screenshots-grid {
      grid-template-columns: 1fr;
    }

    .keybindings-grid {
      grid-template-columns: 1fr;
    }

    .theme-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .step {
      flex-direction: column;
      align-items: center;
      text-align: center;
    }
  }

  @media (max-width: 480px) {
    h1 {
      font-size: 2rem;
    }

    .copy-text {
      display: none;
    }

    .theme-grid {
      grid-template-columns: 1fr 1fr;
    }
  }
</style>

<script>
  // Install tab switching
  document.querySelectorAll('.install-tab').forEach(tab => {
    tab.addEventListener('click', () => {
      const targetTab = tab.getAttribute('data-tab');

      document.querySelectorAll('.install-tab').forEach(t => t.classList.remove('active'));
      tab.classList.add('active');

      document.querySelectorAll('.install-command').forEach(cmd => cmd.classList.remove('active'));
      document.getElementById(`${targetTab}-cmd`)?.classList.add('active');
    });
  });

  // Copy to clipboard
  document.querySelectorAll('.copy-btn').forEach(btn => {
    btn.addEventListener('click', async () => {
      const text = btn.getAttribute('data-copy');
      if (!text) return;

      try {
        await navigator.clipboard.writeText(text);
        btn.classList.add('copied');
        const textEl = btn.querySelector('.copy-text');
        if (textEl) textEl.textContent = 'Copied!';

        setTimeout(() => {
          btn.classList.remove('copied');
          if (textEl) textEl.textContent = 'Copy';
        }, 2000);
      } catch (err) {
        console.error('Failed to copy:', err);
      }
    });
  });
</script>
